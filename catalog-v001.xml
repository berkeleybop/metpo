<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<catalog prefer="public" xmlns="urn:oasis:names:tc:entity:xmlns:xml:catalog">
    <!--
    Workaround for broken MEO ontology import

    MEO (Microbial Electrochemical Ontology) at non-ols/MEO.owl declares an import
    of https://mdatahub.org/data/msv/ which returns unparseable HTML (broken link).

    This catalog redirects the broken import to /dev/null, allowing ROBOT and Protégé
    to work with MEO without failing on the missing import.

    See: docs/NON_OLS_ONTOLOGY_STATUS.md for details
    -->
    <uri name="https://mdatahub.org/data/msv/" uri="file:///dev/null"/>
</catalog>
